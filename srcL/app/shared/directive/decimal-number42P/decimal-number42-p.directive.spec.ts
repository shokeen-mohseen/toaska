import { DecimalNumber42PDirective } from './decimal-number42-p.directive';

describe('DecimalNumber42PDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalNumber42PDirective();
    expect(directive).toBeTruthy();
  });
});
