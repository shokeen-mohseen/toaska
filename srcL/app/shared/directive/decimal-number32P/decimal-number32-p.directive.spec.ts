import { DecimalNumber32PDirective } from './decimal-number32-p.directive';

describe('DecimalNumber32PDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalNumber32PDirective();
    expect(directive).toBeTruthy();
  });
});
