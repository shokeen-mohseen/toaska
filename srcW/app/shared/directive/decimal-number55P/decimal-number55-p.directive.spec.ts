import { DecimalNumber55PDirective } from './decimal-number55-p.directive';

describe('DecimalNumber55PDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalNumber55PDirective();
    expect(directive).toBeTruthy();
  });
});
