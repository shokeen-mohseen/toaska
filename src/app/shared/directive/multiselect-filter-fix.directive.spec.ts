import { MultiselectFilterFixDirective } from './multiselect-filter-fix.directive';

describe('MultiselectFilterFixDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiselectFilterFixDirective();
    expect(directive).toBeTruthy();
  });
});
